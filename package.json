{
    "name": "flask-admin",
    "version": "0.1.0",
    "description": "flask-admin",
    "scripts": {
        "copy-files": "sh scripts/copy-vendor.sh",
        "install-vendor": "npm install",
        "delete-node-modules": "rm -rf node_modules",
        "build": "npm run install-vendor && npm run copy-files"
    },
    "dependencies": {
        "@fortawesome/fontawesome-free": "6.5.2",
        "@tabler/core": "1.0.0-beta20",
        "X-editable": "github:skycyclone/x-editable"
    }
}

[project]
name = "example-s3"
version = "0.1.0"
description = "PyMongo Example."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask-admin[s3,translation]",
    "testcontainers",
]

[tool.uv.sources]
flask-admin = { path = "../../", editable = true }

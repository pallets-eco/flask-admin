[project]
name = "example-sqla-inline-forms"
version = "0.1.0"
description = "SQLAlchemy Custom Inline Forms Example."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask-admin[sqlalchemy]",
]

[tool.uv.sources]
flask-admin = { path = "../../", editable = true }

[project]
name = "example-pymongo"
version = "0.1.0"
description = "PyMongo Example."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "flask-admin[pymongo]",
    "pymongo>=4,<5",
]

[tool.uv.sources]
flask-admin = { path = "../../", editable = true }
